# SSHClient
SSHClient is a local light-weight, database-free SSH client for Windows. It utilises Windows Credentials Manager to store sentisive details and JSON for storing devices (non-sensitive information).

It is very basic at the moment, but is a work in progress with the base model.
