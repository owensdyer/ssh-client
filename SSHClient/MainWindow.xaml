<Window
    x:Class="SSHClient.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SSHClient"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="SSH Client">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <!-- Define three rows: top bar, content & footer -->
        <Grid.RowDefinitions>
            <!-- Top bar height -->
            <RowDefinition Height="Auto"/>
            <!-- Scrollable content fills remaining space -->
            <RowDefinition Height="*"/>
            <!-- Footer -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top bar: Add Device button -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Padding="12" Spacing="8">
            <Button Content="+ Add Device" Click="OpenAddDeviceWindow_Click" Height="36"/>
        </StackPanel>

        <!-- Scrollable device list -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="DevicesPanel" Spacing="8" Padding="12"/>
        </ScrollViewer>

        <!-- Footer -->
        <Border Grid.Row="2"
        Padding="6"
        Background="{ThemeResource SystemControlBackgroundBaseLowBrush}">
            <TextBlock FontSize="11"
               HorizontalAlignment="Center"
               TextAlignment="Center">

        <Run Text="© 2026 Owen Dyer  •  "/>

        <Hyperlink NavigateUri="https://github.com/owensdyer/ssh-client">
            <Run Text="GitHub"/>
        </Hyperlink>

        <Run Text="  •  "/>

        <Hyperlink NavigateUri="https://owendyer.co.uk">
            <Run Text="Website"/>
        </Hyperlink>

            </TextBlock>
        </Border>
    
    </Grid>
</Window>
